# 位置情報連動アラームアプリ 要件定義書

## 1. プロジェクト概要

### 1.1 アプリケーション名
位置情報連動アラームアプリ (Location-Based Alarm)

### 1.2 アプリケーションの目的
指定した場所に到着しない限り、アラームを停止・編集・削除できない仕組みを提供することで、ユーザーが確実に目的地（例：会社、学校、ジム等）に到着することを支援する。

### 1.3 対象プラットフォーム
- Android (推奨: Android 12以上, 最低: Android 8.0以上)
- 個人利用を想定

---

## 2. 機能要件

### 2.1 アラーム追加機能
ユーザーはマップ上で目的地を指定し、新しいアラームを追加できる。
- **目的地設定**: マップ上の任意の地点をピンで指定（デフォルトは現在地）。
- **名前設定**: アラームに名前を付与可能（任意）。
- **時刻設定**: アラーム鳴動時刻を指定（必須）。
- **リモート設定**: アラームを追加する際、その場所にいる必要はない。

### 2.2 アラーム鳴動・停止機能
指定時刻にアラームを鳴らし、目的地に到着している場合のみ停止可能とする。
- **鳴動制御**: アプリが終了していても、`AlarmManager` により正確な時刻に起動。ロック画面上でも `FullScreenIntent` によりアラーム画面を表示する。
- **マナーモード回避**: システムのサウンド設定（マナーモード、おやすみモード等）を無視して音を鳴らすため、オーディオ属性に `USAGE_ALARM` を設定し、適切なオーディオストリームで再生する。
- **音量制御**: アラーム鳴動時はアプリ側で音量を動的に制御し、ユーザーが確実に気づく音量を維持する。
- **停止条件**: 目的地の半径（デフォルト100m）以内にいる場合のみ停止ボタンが有効化。
- **位置取得戦略**: アラーム画面が表示されている間（または停止ボタン押下時）に位置情報を取得し、距離を計算。バッテリー保護のため、常にバックグラウンドで追跡は行わない。
- **連続通知**: 停止されるまで、一定間隔（例：10秒ごと）で通知音またはバイブレーションを継続。

### 2.3 アラーム編集・削除機能
既存のアラームの設定変更・削除を行う機能。
- **編集制限**: 目的地の範囲内（例：半径100m）にいる場合のみ編集・削除画面への遷移を許可。
- **位置確認**: 編集/削除リクエスト時に一時的に位置情報を取得し、条件を満たさない場合は拒否メッセージを表示。

### 2.4 アラーム一覧管理
登録されているアラームのステータスを確認・管理する。
- **表示内容**: 名前、時刻、目的地の簡易住所、有効/無効の状態。
- **トグル切替**: アラームの有効/無効を一覧から直接切り替え可能（編集同様の制限）。

### 2.5 マップ・位置情報連携
- **Google Maps連携**: 目的地の視覚的な指定と、現在地の表示。
- **オンデマンド位置取得**: 必要な瞬間（アラーム鳴動時、編集操作時）のみ `FusedLocationProviderClient` を用いて高精度な位置情報を取得。

---

## 3. 非機能要件

### 3.1 パフォーマンス・信頼性
- **時刻精度**: Androidの `setAlarmClock()` を使用し、Dozeモードやスタンバイ状態でも確実に鳴動させる。
- **鳴動の確実性**: サイレントモードやおやすみモード等、システムの消音設定に関わらず設定された音量でアラームを鳴らす。
- **バッテリー効率**: 常時バックグラウンド追跡を排除し、オンデマンド取得に限定。

### 3.2 セキュリティ・プライバシー
- **データ保存**: `Room` を用いて端末内に安全に保存。外部サーバーへの送信は行わない（個人利用前提）。
- **権限管理**: 最小限の権限（必要な時のみの位置情報）を要求。

### 3.3 ユーザビリティ
- **マテリアルデザイン**: Jetpack Compose (Material3) によるモダンで直感的なUI。
- **フィードバック**: 位置情報が範囲外の場合、あと何メートル必要かを具体的に表示。

---

## 4. 技術要件

### 4.1 開発スタック
- **言語**: Kotlin
- **UI フレームワーク**: **Jetpack Compose** (Material3)
- **アーキテクチャ**: **MVVM** + **Clean Architecture** (UI / Domain / Data)
- **DI (依存性注入)**: **Hilt**
- **非同期・ストリーム**: **Coroutines** & **Flow**
- **データベース**: **Room** (SQLite)

### 4.2 使用API・ライブラリ
- **位置・地図**: 
    - Google Maps SDK for Android
    - Maps Compose (Compose用ラッパー)
    - Google Play Services (Fused Location Provider)
- **システム連携**:
    - `AlarmManager` (正確なアラーム制御)
    - `NotificationManager` (FullScreenIntentを含む通知制御)

---

## 5. UI/UX 構成（想定）

1. **アラーム一覧画面**: 現在登録されているアラーム。FAB（浮遊アクションボタン）から追加。
2. **アラーム設定画面**: 時刻ピッカー、マップ（目的地選択）、名称入力。
3. **アラーム鳴動画面**: ロック画面を覆う全画面表示。現在の距離と停止ボタンを表示。
4. **権限設定ガイド**: 初回起動時に通知や正確なアラーム、位置情報の許可を促す。

---

## 6. 必要な権限 (AndroidManifest.xml)
- `android.permission.ACCESS_FINE_LOCATION` (高精度位置情報)
- `android.permission.POST_NOTIFICATIONS` (Android 13以降)
- `android.permission.SCHEDULE_EXACT_ALARM` (Android 12以降)
- `android.permission.USE_FULL_SCREEN_INTENT` (ロック画面表示)
- `android.permission.FOREGROUND_SERVICE` (鳴動中の位置追跡用)
- `android.permission.WAKE_LOCK` (デバイス起動維持)

---

## 7. 更新履歴
- 2025-12-28: 技術スタックを最新化、位置情報取得戦略をオンデマンド方式に変更、UI要件をJetpack Composeに刷新 (Ver. 1.1)
- 2025-12-28: マナーモード・おやすみモード回避（音量貫通）要件を追加 (Ver. 1.2)

